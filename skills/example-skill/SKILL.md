---
name: example-skill
description: Template skill — replace this. Describe in one sentence WHAT the skill does, then the concrete TRIGGERS that should invoke it (user phrases, a "/slash-command", or a task shape). The description is how the agent decides to use the skill, so make it specific and action-first.
---

# example-skill

Replace this file with your real skill. Keep it portable: standard `name` + `description`
frontmatter only.

## When to use

- Trigger 1 (e.g. the user says "…")
- Trigger 2 (e.g. runs `/example`)

## Steps

1. What to gather / read first.
2. The action to perform (e.g. run a CLI command, call a tool).
3. What to report back.

## Guardrails

- Never do X.
- Always confirm before Y.
