# Features

The single **numbered backlog**: everything the user asks to build and every brainstorm
idea. Numbers are **stable and never reused**. Entries are grouped by state — **Current**
(in progress) · **Planned** · **Brainstorm** (ideas) · **Delivered**. New requests and ideas
land here first, then become tasks in [TODO.md](TODO.md).

## Current (in progress)

_None._

## Planned

_None yet — add your first feature here as `1. **Title.** …`._

## Brainstorm (ideas)

_None yet._

## Delivered

1. **Project scaffold.** Rules (`CLAUDE.md`/`AGENTS.md`), doc-sync + testing policy +
   per-task lifecycle, backlog/CHANGELOG/TODO, `docs/` (configuration + test catalog),
   `auto-tests/` (a/b/c), CI pipeline (quality/security/scanners + GHCR image & chart +
   functional), `Dockerfile` + compose, generic Helm chart, and a portable `SKILL.md`
   template — all wired so the sample app's CI is green out of the box.
