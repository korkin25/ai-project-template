{{- if .Values.gatewayApi.enabled -}}
{{- if not .Values.gatewayApi.parentRefs }}
{{- fail "gatewayApi.enabled=true requires gatewayApi.parentRefs — an HTTPRoute with no parentRef is accepted by the API server and then silently routes nothing" }}
{{- end }}
{{- if not .Values.service.enabled }}
{{- fail "gatewayApi.enabled=true requires service.enabled=true — an HTTPRoute needs a Service to send traffic to" }}
{{- end }}
# Gateway API HTTPRoute — attach the service to an existing Gateway. Preferred over Ingress.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "app.fullname" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
spec:
  parentRefs:
    {{- toYaml .Values.gatewayApi.parentRefs | nindent 4 }}
  {{- with .Values.gatewayApi.hostnames }}
  hostnames:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.gatewayApi.rules }}
    - matches:
        {{- toYaml .matches | nindent 8 }}
      backendRefs:
        - name: {{ include "app.fullname" $ }}
          port: {{ .backendPort | default $.Values.service.port }}
    {{- end }}
{{- end }}
