"""Sample package for the ai-project-template. Replace with your real code."""

# Placeholder only. The real version is injected by release.yml from GitVersion at
# publish time (`hatch version <semver>`) — do NOT hand-maintain this number.
__version__ = "0.0.0"
