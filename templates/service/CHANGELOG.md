# Changelog

All notable changes to @@PROJECT@@ are documented here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and the project
adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html) — with the version
itself computed by GitVersion, never written by hand.

Entries land here **when a task is done and verified**, moved out of `TODO.md` in the same
change. Anything user-visible belongs under `## [Unreleased]` as it is merged, so cutting a
release is a rename of that heading rather than an archaeology exercise.

## [Unreleased]

### Added

- Repository scaffolded from the shared `service` template: composition-only GitLab CI
  pinned to a tagged `open_ci_cd/templates`, `deploy/Dockerfile` + `helm/` at the canonical
  paths, hardened chart, tier-(a) unit and image smoke tests, and the full doc set.

### Changed

### Deprecated

### Removed

### Fixed

### Security
