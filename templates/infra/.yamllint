# yamllint — the first gate a change to this repo passes.
#
# The rules are chosen along one axis: STRICT about anything that silently changes MEANING,
# lenient about anything that is merely style. In an infra repo a YAML quirk is not a
# cosmetic issue — `key: yes` becomes the boolean True, a duplicate key silently discards the
# first value, and a tab makes a file parse differently in two tools. Each of those has taken
# a cluster down somewhere.
extends: default

ignore: |
  .venv/
  rendered/
  .git/

rules:
  # Style, not meaning: long lines in a comment or a long URL in a requirements file are not
  # worth a red pipeline. Warned, not failed.
  line-length:
    max: 120
    level: warning
  # Kubernetes manifests are written both with and without it; forcing one produces churn.
  document-start: disable
  # Comment indentation trips over the block-scalar comments used throughout this repo.
  comments-indentation: disable
  # THE IMPORTANT ONE. Unquoted yes/no/on/off are booleans in YAML 1.1, which many parsers
  # still implement. A version string or a hostname that happens to read as `no` becomes
  # `false`, and the object is admitted with a field nobody wrote.
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
  # A duplicate key does not error in YAML — the last one silently wins. In a values file
  # that is a setting someone believes they made and did not.
  key-duplicates: enable
  octal-values:
    # `mode: 0644` is an integer in YAML; a chart or manifest expecting a string then gets
    # 420. This catches that whole class of file-permission bug.
    forbid-implicit-octal: true
    forbid-explicit-octal: false
